name: "MAPL3 Workflow"
description: "Mapping Axonal Projections in Light-sheet microscopy in 3D"
version: "1.0"

modules:
  conversion@raw: {}
  conversion@registered: {}
  registration@main: {}

execution_order:
  - raw
  - registered
  - main

data_flow:
  raw: {}

  registered:
    input_folder: raw.output_folder
  
  main:
    nii_folder: registered.output_folder
    tiff_folder: registered.tiff_folder
